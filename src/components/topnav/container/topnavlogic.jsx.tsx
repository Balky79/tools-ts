import React, { ReactElement } from 'react';

import TopNavPresentation from '../presentation/topnavpresentation.jsx';

function TopNavContainer(): ReactElement {
    return (
        <div>
            <TopNavPresentation></TopNavPresentation>
        </div>
    );
}

export default TopNavContainer;
